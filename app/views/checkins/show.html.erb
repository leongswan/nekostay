<h1>Checkin 詳細 <span class="meta">(Stay #<%= @stay.id %>)</span></h1>

<div class="card" style="margin-bottom:16px;">
  <dl class="def">
    <dt>記録日時</dt><dd><%= dt_label(@checkin.checked_at) %></dd>
    <dt>体重</dt><dd><%= weight_label(@checkin.weight) %></dd>
    <dt>メモ</dt><dd><%= simple_format(@checkin.memo.presence || "-") %></dd>
  </dl>
</div>

<div class="card">
  <h3 style="margin-top:0;">詳細記録</h3>
  <dl class="def">
    <dt>Meal</dt><dd><%= meal_label(@checkin.meal) %></dd>
    <dt>Litter</dt><dd><%= litter_label(@checkin.litter) %></dd>
    <dt>Meds</dt><dd><%= meds_label(@checkin.meds) %></dd>
  </dl>
</div>

<div class="stack" style="margin-top:16px;">
  <%= link_to "一覧へ戻る", stay_checkins_path(@stay), class: "btn" %>
  <%= link_to "編集", edit_stay_checkin_path(@stay, @checkin), class: "btn btn--primary" %>
  <%= button_to "削除", stay_checkin_path(@stay, @checkin),
        method: :delete,
        form: { data: { turbo_confirm: "削除してよろしいですか？" }, class: "inline" },
        class: "btn btn--danger" %>
</div>
