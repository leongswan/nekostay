<% if user_signed_in? %>
  <div style="max-width: 800px; margin: 40px auto; padding: 0 20px;">
    
    <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 30px;">
      <% if current_user.avatar.attached? %>
        <%= image_tag current_user.avatar, style: "width: 60px; height: 60px; border-radius: 50%; object-fit: cover; border: 2px solid #D98E63;" %>
      <% else %>
        <i class="fa-solid fa-circle-user" style="font-size: 60px; color: #ccc;"></i>
      <% end %>
      <div>
        <h1 style="font-size: 1.5rem; color: #5A4A42; margin: 0;">おかえりなさい、<%= current_user.name %>さん！</h1>
        <p style="color: #888; margin: 0;">シッターさんからの報告や、予約の状況を確認しましょう。</p>
      </div>
    </div>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
      <h2 style="font-size: 1.2rem; border-left: 4px solid #D98E63; padding-left: 10px; margin: 0;">📅 あなたの予約</h2>
      <div style="display: flex; gap: 10px;">
        <%= link_to pets_path, style: "background: #fff; border: 1px solid #D98E63; color: #D98E63; padding: 8px 16px; border-radius: 20px; text-decoration: none; font-size: 0.9rem; font-weight: bold; display: flex; align-items: center; gap: 5px;" do %>
          <i class="fa-solid fa-paw"></i> うちの子リスト
        <% end %>
      <%= link_to "+ 新規予約", new_stay_path, style: "background: #D98E63; color: white; padding: 8px 16px; border-radius: 20px; text-decoration: none; font-size: 0.9rem; font-weight: bold;" %>
    </div>

    <% if @my_stays.present? %>
      <div style="display: grid; gap: 20px;">
        <% @my_stays.each do |stay| %>
          <div class="card" style="padding: 20px; border-radius: 12px; background: white; box-shadow: 0 2px 8px rgba(0,0,0,0.05); border: 1px solid #f0f0f0; transition: transform 0.2s;">
            <div style="display: flex; justify-content: space-between; align-items: start;">
              
              <div>
                <h3 style="margin: 0 0 5px 0; font-size: 1.1rem; color: #333;">
                  <i class="fa-solid fa-paw" style="color: #D98E63;"></i> <%= stay.pet.name %> ちゃん
                </h3>
                <p style="color: #666; font-size: 0.9rem; margin: 0;">
                  <%= stay.start_on %> 〜 <%= stay.end_on %>
                </p>
                
                <% if stay.report_images.attached? %>
                  <div style="display: flex; gap: 5px; margin-top: 10px;">
                    <% stay.report_images.last(3).each do |img| %>
                      <%= image_tag img, style: "width: 40px; height: 40px; border-radius: 6px; object-fit: cover;" %>
                    <% end %>
                    <% if stay.report_images.count > 3 %>
                      <span style="font-size: 0.8rem; color: #999; align-self: center;">+<%= stay.report_images.count - 3 %></span>
                    <% end %>
                  </div>
                <% end %>
              </div>

              <%= link_to "詳細を見る", stay_path(stay), class: "btn", style: "background: #fff; border: 1px solid #D98E63; color: #D98E63; padding: 8px 16px; border-radius: 6px; text-decoration: none; font-size: 0.9rem;" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div style="text-align: center; padding: 40px; background: #f9f9f9; border-radius: 12px; border: 1px dashed #ccc;">
        <p style="color: #666;">まだ予約がありません。</p>
        <p>旅行や出張の予定が決まったら、まずは予約を作成しましょう！</p>
        <%= link_to "最初の予約を作る", new_stay_path, class: "btn", style: "background: #D98E63; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; margin-top: 10px; display: inline-block;" %>
      </div>
    <% end %>

  </div>

 <% else %>
 <section class="hero-section">
  <div class="hero-content">
    <p class="hero-sub-copy">離れていても、心はそばに。<br>愛猫のための優しい見守りアプリ。</p>
    <h1 class="hero-main-copy">いつものお家で、<br>いつもの安心を。<br>Nekostay.</h1>
    <% unless user_signed_in? %>
      <%= link_to "無料で始めてみる", new_user_registration_path, class: "hero-button" %>
    <% else %>
      <%= link_to "マイページへ", stays_path, class: "hero-button" %>
    <% end %>
  </div>
 </section>

 <section class="features-section">
  <div class="container features-container">
    <div class="feature-item">
      <div class="feature-icon-circle">
        <i class="fa-solid fa-camera-retro feature-icon"></i>
      </div>
      <h2 class="feature-title">毎日の様子が見える</h2>
      <p class="feature-desc">『今どうしてるかな？』が写真付きで届くから、外出先でも安心。</p>
    </div>

    <div class="feature-item">
      <div class="feature-icon-circle">
        <i class="fa-solid fa-file-contract feature-icon"></i>
      </div>
      <h2 class="feature-title">約束をカタチに</h2>
      <p class="feature-desc">口約束だけでなく、しっかりとした契約書を。大切な家族を守ります。</p>
    </div>

    <div class="feature-item">
      <div class="feature-icon-circle">
        <i class="fa-solid fa-handshake-simple feature-icon"></i>
      </div>
      <h2 class="feature-title">困った時もスムーズに</h2>
      <p class="feature-desc">面倒なお金のやり取りや、シッター間の連絡もアプリにお任せ。</p>
    </div>
  </div>
 </section>

 <section class="footer-action-section">
  <p>難しい操作はいりません。スマホひとつで、今日から始められます。</p>
 </section>
<% end %>